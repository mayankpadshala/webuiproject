# webuiproject
 
Run below commands to run the project.  
To run client part.
```sh
cd webuiproject/client
npm install
npm start

```

To run server Part.
```sh
cd webuiproject/server
npm install
node index.js

```
